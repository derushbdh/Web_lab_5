extends template

block nav

     nav
        +createMenuBuild(dictMenu, structures[0].menuItem)
    
block content

    img.big(src= require("../images/" + structures[0].pict))

    section.building-text
        p= structures[0].firstP
        p= structures[0].secondP